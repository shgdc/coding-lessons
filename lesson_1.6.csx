// one can write valid expressions that mix values of different types, like 12 + "7"
// but the operators are not defined for mixed types
// therefore, the computer implicitly changes the type of some values following some rules
// guess what the result will be of the expression below
// run it and try to explain the result
Console.WriteLine("12 + 7" + " = " + 12 + 7);